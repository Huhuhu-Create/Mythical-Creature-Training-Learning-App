# 任务：接入用户「一~六年级 语数英」题库（新格式）

## 目标
用户提供了标准题库格式（含 题库信息 + 题目；选项为 A/B/C/D 对象、答案为字母、含 类别/难度/知识点），要求放入文件夹、完全替换旧 27 题简单题库；并明确「上册按上册、下册按下册」严格分离。

## 改造内容
- 新增 `D:\QClaw\1112\banks\` 目录，作为题库根目录。
- `router.php` 重写：
  - `loadBanks()` 扫描 `banks/*.json`，兼容 `{题库信息, 题目}` 或直接题目数组两种结构。
  - `normalizeBank()` 把选项规范为 `[{key,text}]`、答案统一存字母（兼容下标数字），并附 类别/难度/知识点/学科/年级/学期/id。
  - `GET /api/load` 支持 `subject/grade/term` 查询参数过滤；返回 `{state, banks, questions}`。
  - banks 条目展平为可直接渲染的元数据（name/version/subject/grade/term/教材/总数/创建日期/说明/file/count）。
  - **学期严格匹配**：`filterQuestions()` 中 `if ($term && $q['term'] !== $term) return false;` —— 上册只看上册，下册只看下册（不再把「学期为空」的题混入两边）。
- 前端 `index.html`/`app.js`：
  - 学科按钮（语文/数学/英语）+ 年级下拉（一到六）+ 学期下拉（上/下册）。
  - 选中学科后展示命中题库列表（名称/学科/年级/学期/教材/总数）。
  - 答题卡片顶部显示 类别、难度、知识点 标签；选项按 A/B/C/D 渲染。
  - 进度统计按 语文/数学/英语 三科累计，存入 `data/state.json`。
  - 前端 `renderBanks` 与 `startQuiz` 均改为严格按 term 匹配（term为空时才显示全部）。
- 旧 27 题内置题库已移除。

## 用户实际放入的题库（重要）
用户把真实题库放进了 `banks/`，最终共 **37 个文件**：
- 36 份真实题库：一年级~六年级 × 语文/数学/英语 × 上册/下册。
- 1 份示例：`sample_一年级数学上册.json`（3 题，格式参考，可删）。
- 全量校验结果（GET /api/load）：
  - 题库份数 37，题目总数 1356。
  - 学期分布：上册 658、下册 698。
  - 异常统计：题干为空 0、无答案 0、无选项 0、答案不在选项内 0 → 全部正常。

## 修复的 bug（历史）
1. 静态文件 MIME 用 `.$ext` 查表（之前下载主页问题）。
2. bank 元数据 `info` 嵌套导致前端读不到 → 已展平。
3. 学期匹配原为「term 为空也显示」，改为严格匹配（上册/下册分离）。

## 待清理（删除被安全策略拦截，需用户确认）
临时测试脚本：`_test.js`、`_t2.js`、`_t3.js`、`_validate.js`（均为本次调试遗留，无害）。
另：`sample_一年级数学上册.json` 为示例，真实题库已就位，可保留作格式参考或删除。
`.idea` 目录非本项目创建，未处理。
